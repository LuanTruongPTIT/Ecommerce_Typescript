export enum UserVerifyStatus {
  Unverified,
  Verified,
  Banned
}
export enum TokenType {
  AccessToken,
  RefreshToken,
  forgotPasswordToken,
  EmailVerifyToken
}
export enum MediaType {
  Image,
  Video,
  Hls
}
